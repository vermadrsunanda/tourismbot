[build]
packages = ["."]

[tool.poetry.dependencies]
python = "^3.8"
streamlit = "^1.28.0"
mistralai = "^0.3.0"
requests = "^2.31.0"
python-dotenv = "^1.0.0"
pydantic = "^2.5.0"

[tool.poetry.dev-dependencies]
pytest = "^7.4.0"
